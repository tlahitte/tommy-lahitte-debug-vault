---
phase: 01-design-system
plan: 04
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - src/components/blog/BlogCard.tsx
  - src/app/template.tsx
autonomous: false
requirements:
  - DSGN-02
  - DSGN-03

must_haves:
  truths:
    - "BlogCard uses semantic tokens — no zinc or violet classes remain"
    - "BlogCard lifts translateY(-2px) with warm rust shadow on hover via Framer Motion whileHover"
    - "BlogCard placeholder gradient uses surface colors instead of violet/zinc"
    - "Page transitions work: every route change triggers a fade-up entry animation (opacity 0→1, y 8→0, 250ms)"
    - "motion package is installed and importable from 'motion/react'"
    - "template.tsx exists at src/app/template.tsx with 'use client' and motion.div entry animation"
  artifacts:
    - path: "src/app/template.tsx"
      provides: "Page transition wrapper — re-renders on every navigation"
      contains: "motion.div"
    - path: "src/components/blog/BlogCard.tsx"
      provides: "BlogCard with semantic tokens and Framer Motion hover"
      contains: "whileHover"
  key_links:
    - from: "src/app/template.tsx"
      to: "motion/react"
      via: "import { motion } from 'motion/react'"
      pattern: "from 'motion/react'"
    - from: "BlogCard.tsx"
      to: "motion/react"
      via: "motion.div whileHover"
      pattern: "whileHover.*y: -2"
---

<objective>
Install the `motion` package, rebuild BlogCard.tsx with semantic tokens and Framer Motion hover interaction, and create src/app/template.tsx for page entry animations. This plan delivers DSGN-03 (micro-animations) and completes the BlogCard color migration (DSGN-02).

Purpose: BlogCard is the primary visual element on the homepage and blog listing — its hover interaction is the most visible UX improvement. The template.tsx page transition gives the site a sense of craft and intentionality on every navigation.
Output: motion installed, BlogCard with whileHover lift, template.tsx with entry animation.
</objective>

<execution_context>
@/Users/tommy.lahitte/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tommy.lahitte/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-design-system/01-CONTEXT.md
@.planning/phases/01-design-system/01-RESEARCH.md
@.planning/phases/01-design-system/01-UI-SPEC.md
@.planning/phases/01-design-system/01-01-SUMMARY.md

<interfaces>
<!-- BlogCard.tsx currently imports: -->
<!--   'use client' (already a client component — safe to add motion) -->
<!--   import Link from 'next/link' -->
<!--   import Image from 'next/image' -->
<!--   import type { BlogPost } from '@/lib/blog-types' -->

<!-- BlogPost type (from @/lib/blog-types): -->
<!--   slug: string | date: string | title: string | excerpt: string -->
<!--   image?: string | imageAlt?: string | link?: { url: string, label: string } -->

<!-- Token classes available (from Plan 01): -->
<!--   bg-surface-raised | text-text-muted | text-accent | hover:text-accent | border-border -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install motion package and create template.tsx</name>
  <files>src/app/template.tsx</files>
  <read_first>
    - .planning/phases/01-design-system/01-RESEARCH.md §Pattern 3: Page Transitions via template.tsx — exact implementation
    - .planning/phases/01-design-system/01-RESEARCH.md §Pitfall 2: motion/react import vs framer-motion — DO NOT install framer-motion
    - .planning/phases/01-design-system/01-RESEARCH.md §Pitfall 3: Motion Components in Server Components — template.tsx MUST have 'use client'
    - package.json — verify motion is not already installed before running npm install
  </read_first>
  <action>
**Step 1 — Install the motion package:**

```bash
npm install motion
```

Verify with `npm ls motion` — should show `motion@12.x.x`. If `framer-motion` also appears, that is a problem — but it should not be in this project (there is no pre-existing animation library).

Do NOT run `npm install framer-motion` — the canonical package is `motion`. The import path is `motion/react`.

**Step 2 — Create src/app/template.tsx:**

This file does NOT exist yet — create it fresh. It must have `'use client'` as the first line (motion.div requires browser APIs; Server Components cannot use it).

```tsx
'use client'

import { motion } from 'motion/react'

export default function Template({ children }: { children: React.ReactNode }) {
  return (
    <motion.div
      initial={{ opacity: 0, y: 8 }}
      animate={{ opacity: 1, y: 0 }}
      transition={{ duration: 0.25, ease: 'easeOut' }}
    >
      {children}
    </motion.div>
  )
}
```

Key implementation details:
- `initial={{ opacity: 0, y: 8 }}` — starts slightly below, invisible
- `animate={{ opacity: 1, y: 0 }}` — slides up and fades in
- `duration: 0.25` — 250ms per locked decision (UI-SPEC §Interaction Contract: Page transitions)
- `ease: 'easeOut'` — decelerates into final position, feels natural
- `template.tsx` re-renders on EVERY navigation (unlike `layout.tsx` which persists) — this is the stable Next.js App Router pattern for page entry animations
- Do NOT add AnimatePresence here — entry-only animations via template.tsx, no exit animations (per RESEARCH.md §Anti-Patterns)
  </action>
  <verify>
    <automated>ls /Users/tommy.lahitte/Documents/Github/tommy-lahitte-debug-vault/src/app/template.tsx</automated>
    <automated>grep "'use client'" /Users/tommy.lahitte/Documents/Github/tommy-lahitte-debug-vault/src/app/template.tsx</automated>
    <automated>grep "from 'motion/react'" /Users/tommy.lahitte/Documents/Github/tommy-lahitte-debug-vault/src/app/template.tsx</automated>
    <automated>grep "opacity: 0, y: 8" /Users/tommy.lahitte/Documents/Github/tommy-lahitte-debug-vault/src/app/template.tsx</automated>
    <automated>cd /Users/tommy.lahitte/Documents/Github/tommy-lahitte-debug-vault && npm ls motion 2>&1 | grep "motion@"</automated>
    <automated>cd /Users/tommy.lahitte/Documents/Github/tommy-lahitte-debug-vault && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - src/app/template.tsx exists
    - template.tsx first line is `'use client'`
    - template.tsx contains `import { motion } from 'motion/react'`
    - template.tsx contains `initial={{ opacity: 0, y: 8 }}`
    - template.tsx contains `animate={{ opacity: 1, y: 0 }}`
    - template.tsx contains `duration: 0.25`
    - `npm ls motion` shows motion@12.x.x installed
    - `npm ls motion` does NOT show framer-motion (no duplicate)
    - `npm run build` exits 0 (motion imports resolve, no TypeScript errors)
  </acceptance_criteria>
  <done>motion package installed from npm, template.tsx created with entry fade-up animation, build passes.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate BlogCard.tsx to semantic tokens with Framer Motion hover</name>
  <files>src/components/blog/BlogCard.tsx</files>
  <read_first>
    - src/components/blog/BlogCard.tsx — current state (already 'use client'; zinc/violet classes; CSS hover via Tailwind; placeholder gradient uses violet/zinc)
    - .planning/phases/01-design-system/01-RESEARCH.md §Pattern 4: Card Hover with Framer Motion — exact whileHover implementation
    - .planning/phases/01-design-system/01-UI-SPEC.md §Component Migration Contract: BlogCard row — full list of replacements
    - .planning/phases/01-design-system/01-UI-SPEC.md §Interaction Contract: Card hover — exact shadow value
  </read_first>
  <action>
**Replace the entire BlogCard.tsx** with the following. The current file uses CSS hover transitions on the outer Link — these must be replaced with a Framer Motion `motion.article` wrapping the card content, while the Link becomes a plain wrapper.

```tsx
'use client'

import Link from 'next/link'
import Image from 'next/image'
import { motion } from 'motion/react'
import type { BlogPost } from '@/lib/blog-types'

interface BlogCardProps {
  post: BlogPost
}

export default function BlogCard({ post }: BlogCardProps) {
  const formattedDate = new Date(post.date).toLocaleDateString('en-GB', {
    year: 'numeric',
    month: 'long',
    day: 'numeric',
  })

  return (
    <Link href={`/blog/${post.slug}`} className="group block">
      <motion.article
        className="rounded-xl border border-border bg-surface-raised overflow-hidden cursor-pointer"
        whileHover={{ y: -2, boxShadow: '0 8px 24px -4px rgba(200, 90, 58, 0.12)' }}
        transition={{ duration: 0.2, ease: 'easeOut' }}
      >
        {post.image ? (
          <div className="relative h-52 w-full">
            <Image
              src={post.image}
              alt={post.imageAlt ?? post.title}
              fill
              className="object-cover"
              sizes="(max-width: 768px) 100vw, 50vw"
            />
          </div>
        ) : (
          <div className="h-52 w-full bg-gradient-to-br from-surface-raised via-surface to-surface flex items-end p-5">
            <span className="text-xs font-medium uppercase tracking-widest text-text-muted">
              Current Work
            </span>
          </div>
        )}
        <div className="p-6">
          <time dateTime={post.date} className="text-xs text-text-muted">
            {formattedDate}
          </time>
          <h2 className="mt-2 text-lg font-semibold text-text-primary group-hover:text-accent transition-colors leading-snug">
            {post.title}
          </h2>
          <p className="mt-2 text-sm text-text-muted leading-relaxed line-clamp-3">{post.excerpt}</p>
          <div className="mt-4 flex items-center gap-4">
            <span className="text-sm text-accent font-medium">Read more</span>
            {post.link && (
              <a
                href={post.link.url}
                target="_blank"
                rel="noopener noreferrer"
                onClick={(e) => e.stopPropagation()}
                className="text-sm text-text-muted hover:text-text-primary transition-colors"
              >
                {post.link.label} &rarr;
              </a>
            )}
          </div>
        </div>
      </motion.article>
    </Link>
  )
}
```

Key changes from the original:
- Added `import { motion } from 'motion/react'`
- Outer `<Link>` loses all hover/transition/border/bg classes — becomes a plain wrapper with `className="group block"`
- Inner `<article>` becomes `<motion.article>` with `whileHover` and `transition` props
- `border-zinc-800` → `border-border`
- `bg-zinc-900/50` → `bg-surface-raised`
- Removed `hover:border-violet-700/60 hover:-translate-y-1 hover:shadow-[...]` from Link (Motion handles this now)
- Placeholder gradient: `from-violet-950 via-zinc-900 to-zinc-800` → `from-surface-raised via-surface to-surface`
- Placeholder label: `text-violet-400` → `text-text-muted`
- Date: `text-zinc-500` → `text-text-muted`
- Title: `text-zinc-100` → `text-text-primary`, `group-hover:text-violet-400` → `group-hover:text-accent`
- Excerpt: `text-zinc-400` → `text-text-muted`
- "Read more": `text-violet-400` → `text-accent`
- External link: `text-zinc-500 hover:text-zinc-300` → `text-text-muted hover:text-text-primary`
- Box shadow tint: `rgba(200, 90, 58, 0.12)` — warm rust, matches accent #C85A3A
  </action>
  <verify>
    <automated>grep "from 'motion/react'" /Users/tommy.lahitte/Documents/Github/tommy-lahitte-debug-vault/src/components/blog/BlogCard.tsx</automated>
    <automated>grep "whileHover" /Users/tommy.lahitte/Documents/Github/tommy-lahitte-debug-vault/src/components/blog/BlogCard.tsx</automated>
    <automated>grep "y: -2" /Users/tommy.lahitte/Documents/Github/tommy-lahitte-debug-vault/src/components/blog/BlogCard.tsx</automated>
    <automated>grep "rgba(200, 90, 58, 0.12)" /Users/tommy.lahitte/Documents/Github/tommy-lahitte-debug-vault/src/components/blog/BlogCard.tsx</automated>
    <automated>grep -E "text-zinc|bg-zinc|border-zinc|text-violet|bg-violet|hover:text-violet|hover:border-violet|hover:bg-violet" /Users/tommy.lahitte/Documents/Github/tommy-lahitte-debug-vault/src/components/blog/BlogCard.tsx</automated>
    <automated>cd /Users/tommy.lahitte/Documents/Github/tommy-lahitte-debug-vault && npm run build 2>&1 | tail -5</automated>
    <automated>cd /Users/tommy.lahitte/Documents/Github/tommy-lahitte-debug-vault && npm run lint 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - BlogCard.tsx first line is `'use client'`
    - BlogCard.tsx contains `import { motion } from 'motion/react'`
    - BlogCard.tsx contains `<motion.article` element
    - BlogCard.tsx contains `whileHover={{ y: -2, boxShadow: '0 8px 24px -4px rgba(200, 90, 58, 0.12)' }}`
    - BlogCard.tsx contains `transition={{ duration: 0.2, ease: 'easeOut' }}`
    - BlogCard.tsx contains `border-border` on the motion.article
    - BlogCard.tsx contains `bg-surface-raised` on the motion.article
    - BlogCard.tsx contains `from-surface-raised` in the placeholder gradient
    - BlogCard.tsx contains `text-accent` for the "Read more" span
    - BlogCard.tsx does NOT contain `text-zinc-`, `bg-zinc-`, `border-zinc-`, `text-violet-`, `bg-violet-`, `hover:text-violet-`, `hover:border-violet-`
    - BlogCard.tsx does NOT contain `hover:-translate-y-1` or `hover:shadow-[` (old CSS hover replaced by Motion)
    - `npm run build` exits 0
    - `npm run lint` exits 0
  </acceptance_criteria>
  <done>BlogCard uses semantic tokens, Framer Motion whileHover lift with rust shadow, placeholder gradient uses surface colors. Zero zinc/violet residue.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Verify page transitions and BlogCard hover interaction</name>
  <action>Run `npm run dev`, open http://localhost:3000, and follow the verification steps below.</action>
  <what-built>
    - motion package installed
    - template.tsx: fade-up entry animation on every page navigation
    - BlogCard.tsx: semantic tokens + physical lift hover effect with rust shadow
  </what-built>
  <how-to-verify>
    Run: `npm run dev` then open http://localhost:3000

    1. Navigate between pages (e.g., Home → Blog → Home): each page should fade in from slightly below over 250ms — a subtle but perceptible entry animation
    2. On the homepage blog cards: hover over a card — it should lift ~2px upward with a warm rust-tinted shadow appearing underneath
    3. Check BlogCard colors: card background should be warm off-white (#F0EBE0), border subtle beige, date/excerpt text warm gray (#7A6F68), title dark (#1A1816)
    4. Hover a card title: title text turns rust/terra cotta (#C85A3A)
    5. "Read more" text should be rust (#C85A3A), not violet
    6. Run: `npm run build && npm run lint` — both should exit 0
  </how-to-verify>
  <resume-signal>Type "approved" or describe any visual issues</resume-signal>
</task>

</tasks>

<verification>
Full audit after both tasks:

1. motion installed: `npm ls motion` shows motion@12.x.x, no framer-motion
2. template.tsx exists: `ls src/app/template.tsx` returns the file
3. BlogCard clean: `grep -E "text-zinc|bg-zinc|border-zinc|text-violet" src/components/blog/BlogCard.tsx` returns 0 lines
4. whileHover present: `grep "whileHover" src/components/blog/BlogCard.tsx` matches
5. Build + lint: `npm run build && npm run lint` both exit 0
</verification>

<success_criteria>
- motion@12.x installed, no framer-motion duplicate
- src/app/template.tsx created with 'use client', motion.div, initial/animate/transition props per UI-SPEC
- BlogCard.tsx uses semantic tokens (border-border, bg-surface-raised, text-text-muted, text-accent)
- BlogCard hover uses Framer Motion whileHover with y: -2 and rgba(200, 90, 58, 0.12) shadow
- Zero zinc/violet classes in BlogCard.tsx
- npm run build and npm run lint pass
</success_criteria>

<output>
After completion, create `.planning/phases/01-design-system/01-04-SUMMARY.md`
</output>
