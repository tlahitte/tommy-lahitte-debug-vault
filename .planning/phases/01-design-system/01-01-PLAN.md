---
phase: 01-design-system
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tailwind.config.ts
  - src/app/globals.css
  - src/app/layout.tsx
autonomous: true
requirements:
  - DSGN-01

must_haves:
  truths:
    - "All 9 semantic CSS custom properties are defined in globals.css :root"
    - "All 9 Tailwind color tokens are available as utility classes via tailwind.config.ts"
    - "The page body background is #FAFAF7 (warm white), not the old #09090b (dark)"
    - "Old dark CSS vars (--background: #09090b, --foreground: #f4f4f5, --accent: #8b5cf6) are replaced"
  artifacts:
    - path: "tailwind.config.ts"
      provides: "9-token semantic color system available as Tailwind classes"
      contains: "surface, surface-raised, text-primary, text-muted, accent, accent-hover, border"
    - path: "src/app/globals.css"
      provides: ":root CSS custom properties backing all 9 tokens"
      contains: "--surface: #FAFAF7"
    - path: "src/app/layout.tsx"
      provides: "Root body class using semantic tokens"
      contains: "bg-surface text-text-primary"
  key_links:
    - from: "src/app/globals.css :root"
      to: "tailwind.config.ts theme.extend.colors"
      via: "var(--token) references"
      pattern: "var\\(--surface\\)"
    - from: "tailwind.config.ts"
      to: "src/app/layout.tsx"
      via: "bg-surface class"
      pattern: "bg-surface"
---

<objective>
Lay the semantic token foundation: define 9 CSS custom properties in globals.css and bridge them into Tailwind as named color utilities. Update layout.tsx body to use the new tokens. This plan must complete before any component migration begins — it is the prerequisite for all Wave 2 plans.

Purpose: Establish the kraft/industrial palette as the single source of truth. Every subsequent component will use these class names, never raw hex or zinc/violet Tailwind classes.
Output: tailwind.config.ts with 9 new color keys, globals.css with kraft :root vars, layout.tsx using bg-surface.
</objective>

<execution_context>
@/Users/tommy.lahitte/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tommy.lahitte/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-design-system/01-CONTEXT.md
@.planning/phases/01-design-system/01-RESEARCH.md
@.planning/phases/01-design-system/01-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define semantic tokens in tailwind.config.ts and globals.css</name>
  <files>tailwind.config.ts, src/app/globals.css</files>
  <read_first>
    - tailwind.config.ts — current state (has only `background` and `foreground` tokens; must extend, not replace)
    - src/app/globals.css — current state (has old dark :root vars; entire :root block must be replaced)
  </read_first>
  <action>
**tailwind.config.ts** — extend `theme.extend.colors` with 7 new keys alongside existing `background` and `foreground`:

```typescript
import type { Config } from 'tailwindcss'

const config: Config = {
  content: [
    './src/pages/**/*.{js,ts,jsx,tsx,mdx}',
    './src/components/**/*.{js,ts,jsx,tsx,mdx}',
    './src/app/**/*.{js,ts,jsx,tsx,mdx}',
  ],
  theme: {
    extend: {
      colors: {
        background: 'var(--background)',
        foreground: 'var(--foreground)',
        surface: 'var(--surface)',
        'surface-raised': 'var(--surface-raised)',
        'text-primary': 'var(--text-primary)',
        'text-muted': 'var(--text-muted)',
        accent: 'var(--accent)',
        'accent-hover': 'var(--accent-hover)',
        border: 'var(--border)',
      },
      fontFamily: {
        display: ['var(--font-inter)', 'sans-serif'],
      },
      animation: {
        'spin-slow': 'spin 6s linear infinite',
      },
    },
  },
  plugins: [],
}

export default config
```

**src/app/globals.css** — replace the entire `:root { ... }` block (lines 5-9) with the kraft palette. Keep all other rules (body, html, .badge-glow, @keyframes, pre) unchanged:

```css
:root {
  /* Legacy aliases — kept for backwards compat, updated values */
  --background: #FAFAF7;
  --foreground: #1A1816;
  /* Semantic kraft palette */
  --surface: #FAFAF7;
  --surface-raised: #F0EBE0;
  --text-primary: #1A1816;
  --text-muted: #7A6F68;
  --accent: #C85A3A;
  --accent-hover: #A8432A;
  --border: #E2D9CE;
}
```

The body rule in globals.css already uses `var(--background)` and `var(--foreground)` — these will automatically reflect the new kraft values without any change to the body rule itself.
  </action>
  <verify>
    <automated>grep -c "surface\|surface-raised\|text-primary\|text-muted\|accent-hover\|border" /Users/tommy.lahitte/Documents/Github/tommy-lahitte-debug-vault/tailwind.config.ts</automated>
    <automated>grep "surface: #FAFAF7" /Users/tommy.lahitte/Documents/Github/tommy-lahitte-debug-vault/src/app/globals.css</automated>
    <automated>grep -c "#09090b\|#f4f4f5\|#8b5cf6" /Users/tommy.lahitte/Documents/Github/tommy-lahitte-debug-vault/src/app/globals.css</automated>
  </verify>
  <acceptance_criteria>
    - tailwind.config.ts contains all 9 keys: `grep "surface\|surface-raised\|text-primary\|text-muted\|accent.*var\|accent-hover\|border.*var" tailwind.config.ts` returns 7 lines
    - globals.css :root contains `--surface: #FAFAF7`
    - globals.css :root contains `--surface-raised: #F0EBE0`
    - globals.css :root contains `--text-primary: #1A1816`
    - globals.css :root contains `--text-muted: #7A6F68`
    - globals.css :root contains `--accent: #C85A3A`
    - globals.css :root contains `--accent-hover: #A8432A`
    - globals.css :root contains `--border: #E2D9CE`
    - globals.css does NOT contain `#09090b` (old dark background gone)
    - globals.css does NOT contain `#8b5cf6` (old violet accent gone)
    - tailwind.config.ts still contains `background: 'var(--background)'` (legacy alias preserved)
    - tailwind.config.ts still contains `'spin-slow': 'spin 6s linear infinite'` (animation preserved)
  </acceptance_criteria>
  <done>9 CSS custom properties defined in globals.css :root; 9 Tailwind color tokens declared in tailwind.config.ts; old dark values removed.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate layout.tsx body class to semantic tokens</name>
  <files>src/app/layout.tsx</files>
  <read_first>
    - src/app/layout.tsx — current state (body uses `bg-zinc-950 text-zinc-100`; only this class string changes, nothing else)
  </read_first>
  <action>
In `src/app/layout.tsx`, find line 76:
```tsx
<body className="bg-zinc-950 text-zinc-100 flex flex-col min-h-screen">
```
Replace with:
```tsx
<body className="bg-surface text-text-primary flex flex-col min-h-screen">
```

That is the only change to this file. All metadata, schema JSON-LD, Inter font setup, Header/Footer imports, and the `<main>` element remain exactly as-is.
  </action>
  <verify>
    <automated>grep "bg-surface text-text-primary" /Users/tommy.lahitte/Documents/Github/tommy-lahitte-debug-vault/src/app/layout.tsx</automated>
    <automated>grep "bg-zinc-950\|text-zinc-100" /Users/tommy.lahitte/Documents/Github/tommy-lahitte-debug-vault/src/app/layout.tsx</automated>
    <automated>cd /Users/tommy.lahitte/Documents/Github/tommy-lahitte-debug-vault && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - layout.tsx contains `bg-surface text-text-primary flex flex-col min-h-screen` on the body element
    - layout.tsx does NOT contain `bg-zinc-950` or `text-zinc-100`
    - `npm run build` exits 0 (no TypeScript errors introduced)
    - `npm run lint` exits 0
    - All other layout.tsx content is unchanged: Inter font setup, metadata export, siteSchemas, Header/Footer imports, `<main className="flex-1 pt-14">` structure
  </acceptance_criteria>
  <done>layout.tsx body class uses semantic tokens; build and lint pass; page background will render as #FAFAF7.</done>
</task>

</tasks>

<verification>
After both tasks complete:

1. Token coverage: `grep -E "surface|surface-raised|text-primary|text-muted|accent|accent-hover|border" tailwind.config.ts | wc -l` must return 9
2. No old dark values in globals.css: `grep -c "#09090b\|zinc-950\|8b5cf6" src/app/globals.css` must return 0
3. layout.tsx uses semantic tokens: `grep "bg-surface text-text-primary" src/app/layout.tsx` must match
4. Build passes: `npm run build && npm run lint` exits 0
</verification>

<success_criteria>
- tailwind.config.ts has 9 semantic color tokens (background, foreground, surface, surface-raised, text-primary, text-muted, accent, accent-hover, border) all bridged via var(--token)
- globals.css :root defines all 9 CSS custom properties with kraft/industrial values
- layout.tsx body renders with bg-surface (#FAFAF7) and text-text-primary (#1A1816)
- No hardcoded dark values remain in the three modified files
- npm run build and npm run lint both pass
</success_criteria>

<output>
After completion, create `.planning/phases/01-design-system/01-01-SUMMARY.md`
</output>
