---
phase: 01-design-system
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - src/components/layout/Header.tsx
autonomous: false
requirements:
  - DSGN-02

must_haves:
  truths:
    - "Header shows 'Tommy Lahitte' as text brand at the left on all viewport widths"
    - "Nav links are: Blog → Projects → About (in that order, right side)"
    - "Header is fully transparent at the top of the page (scrollY === 0)"
    - "Header transitions to bg-surface with a bottom border when scrollY > 48px"
    - "All nav link colors use semantic tokens — no zinc or violet classes remain"
    - "Nav link hover color is accent (#C85A3A), not violet"
  artifacts:
    - path: "src/components/layout/Header.tsx"
      provides: "Rebuilt header with brand identity, scroll behavior, new nav order"
      contains: "Tommy Lahitte"
      exports: ["Header (default)"]
  key_links:
    - from: "Header.tsx useState/useEffect"
      to: "window.scrollY > 48"
      via: "scroll event listener passive: true"
      pattern: "scrollY > 48"
    - from: "Header.tsx className"
      to: "bg-surface border-border"
      via: "scrolled conditional class"
      pattern: "scrolled.*bg-surface"
---

<objective>
Full rebuild of Header.tsx: new kraft/industrial identity with "Tommy Lahitte" brand left, Blog/Projects/About nav right, scroll-aware background transition from transparent to bg-surface at 48px threshold.

Purpose: The header is the first visual element every visitor sees. It must signal the maker identity immediately and work correctly on all viewport widths without a hamburger menu.
Output: A fully rebuilt Header.tsx that is a client component with scroll state, semantic token classes only.
</objective>

<execution_context>
@/Users/tommy.lahitte/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tommy.lahitte/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-design-system/01-CONTEXT.md
@.planning/phases/01-design-system/01-RESEARCH.md
@.planning/phases/01-design-system/01-UI-SPEC.md
@.planning/phases/01-design-system/01-01-SUMMARY.md

<interfaces>
<!-- From tailwind.config.ts (established in Plan 01): -->
<!-- Available token classes: bg-surface, bg-surface-raised, text-text-primary, text-text-muted, text-accent, hover:text-accent, border-border, accent-hover -->
<!-- These are safe to use — Plan 01 must complete before this plan runs. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rebuild Header.tsx with brand identity and scroll behavior</name>
  <files>src/components/layout/Header.tsx</files>
  <read_first>
    - src/components/layout/Header.tsx — current state (static Server Component, no scroll state, links: Home/Tips/Blog, zinc/violet classes, brand absent)
    - .planning/phases/01-design-system/01-RESEARCH.md §Pattern 2: Scroll-Aware Header — exact implementation reference
    - .planning/phases/01-design-system/01-UI-SPEC.md §Interaction Contract: Header scroll behavior — exact class transitions
  </read_first>
  <action>
**Replace the entire file** with the following implementation. The existing file is a static Server Component with 3 links (Home, Tips, Blog) and no brand name — it must be completely replaced.

```tsx
'use client'

import { useState, useEffect } from 'react'
import Link from 'next/link'

export default function Header() {
  const [scrolled, setScrolled] = useState(false)

  useEffect(() => {
    const handleScroll = () => setScrolled(window.scrollY > 48)
    window.addEventListener('scroll', handleScroll, { passive: true })
    return () => window.removeEventListener('scroll', handleScroll)
  }, [])

  return (
    <header
      className={`fixed top-0 inset-x-0 z-50 transition-all duration-300 ${
        scrolled
          ? 'bg-surface border-b border-border'
          : 'bg-transparent border-b border-transparent'
      }`}
    >
      <div className="mx-auto max-w-5xl px-4 sm:px-6">
        <div className="flex h-14 items-center justify-between">
          <Link
            href="/"
            className="text-sm font-semibold text-text-primary hover:text-accent transition-colors focus-visible:outline-2 focus-visible:outline-accent focus-visible:outline-offset-2"
          >
            Tommy Lahitte
          </Link>
          <nav className="flex items-center gap-4 sm:gap-6" aria-label="Main navigation">
            <Link
              href="/blog"
              className="text-sm font-medium text-text-muted hover:text-accent transition-colors focus-visible:outline-2 focus-visible:outline-accent focus-visible:outline-offset-2"
            >
              Blog
            </Link>
            <Link
              href="/projects"
              className="text-sm font-medium text-text-muted hover:text-accent transition-colors focus-visible:outline-2 focus-visible:outline-accent focus-visible:outline-offset-2"
            >
              Projects
            </Link>
            <Link
              href="/about"
              className="text-sm font-medium text-text-muted hover:text-accent transition-colors focus-visible:outline-2 focus-visible:outline-accent focus-visible:outline-offset-2"
            >
              About
            </Link>
          </nav>
        </div>
      </div>
    </header>
  )
}
```

Key decisions embedded in this implementation:
- `'use client'` — required because useState/useEffect are React hooks; Server Components cannot use hooks
- `window.scrollY > 48` — scroll threshold per locked decision (UI-SPEC §Spacing: 48px exception)
- `{ passive: true }` — browser performance optimization for scroll listeners; never omit
- `justify-between` on the inner flex row — places brand left, nav right
- Brand text: `text-sm font-semibold text-text-primary` — nav links: `text-sm font-medium text-text-muted`
- No hamburger: all 3 links visible at all viewports, compressed with `gap-4 sm:gap-6`
- Nav order: Blog, Projects, About — exactly as locked in CONTEXT.md
- Links to `/projects` and `/about` are included even though pages don't exist yet (will 404 until Phase 3/4)
- Focus states use `focus-visible:outline-accent` for accessibility coherence with accent palette
  </action>
  <verify>
    <automated>grep "Tommy Lahitte" /Users/tommy.lahitte/Documents/Github/tommy-lahitte-debug-vault/src/components/layout/Header.tsx</automated>
    <automated>grep "scrollY > 48" /Users/tommy.lahitte/Documents/Github/tommy-lahitte-debug-vault/src/components/layout/Header.tsx</automated>
    <automated>grep -E "text-zinc|bg-zinc|text-violet|hover:text-violet" /Users/tommy.lahitte/Documents/Github/tommy-lahitte-debug-vault/src/components/layout/Header.tsx</automated>
    <automated>grep "href=\"/projects\"" /Users/tommy.lahitte/Documents/Github/tommy-lahitte-debug-vault/src/components/layout/Header.tsx</automated>
    <automated>grep "href=\"/about\"" /Users/tommy.lahitte/Documents/Github/tommy-lahitte-debug-vault/src/components/layout/Header.tsx</automated>
    <automated>cd /Users/tommy.lahitte/Documents/Github/tommy-lahitte-debug-vault && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Header.tsx first line is `'use client'`
    - Header.tsx contains `useState` and `useEffect` imports from 'react'
    - Header.tsx contains `window.scrollY > 48`
    - Header.tsx contains `{ passive: true }` on the scroll event listener
    - Header.tsx contains the exact string `Tommy Lahitte` as link text (not just in a comment)
    - Header.tsx contains `href="/blog"`, `href="/projects"`, `href="/about"` — in that order
    - Header.tsx contains `bg-surface border-b border-border` in the scrolled conditional class
    - Header.tsx does NOT contain `text-zinc-`, `bg-zinc-`, `text-violet-`, `hover:text-violet-`
    - Header.tsx does NOT contain `href="/tips"` or `href="/"` as a nav link (Home and Tips links removed; brand "/" link is the only root href)
    - `npm run build` exits 0
    - `npm run lint` exits 0
  </acceptance_criteria>
  <done>"Tommy Lahitte" brand at left, Blog/Projects/About nav at right, transparent-to-opaque scroll transition at 48px, all semantic tokens, no zinc/violet classes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Verify Header visual identity and scroll behavior</name>
  <action>Run `npm run dev`, open http://localhost:3000, and follow the verification steps below.</action>
  <what-built>
    Rebuilt Header with kraft/industrial identity: "Tommy Lahitte" brand left, Blog/Projects/About nav right, transparent → bg-surface transition on scroll.
  </what-built>
  <how-to-verify>
    Run: `npm run dev` then open http://localhost:3000

    1. Verify at page top: header background is TRANSPARENT (you can see the page content/background through it)
    2. Scroll down more than ~50px: header background becomes warm white (#FAFAF7) with a subtle bottom border
    3. Scroll back to top: header returns to transparent
    4. Check left side: "Tommy Lahitte" is visible as text, not a logo
    5. Check right side: links read "Blog", "Projects", "About" — in that order left to right
    6. Hover a nav link: color transitions to rust/terra cotta (#C85A3A), not violet
    7. On mobile (resize browser < 640px): all 3 nav links are still visible (no hamburger)
  </how-to-verify>
  <resume-signal>Type "approved" or describe any visual issues</resume-signal>
</task>

</tasks>

<verification>
After task 1 completes:
1. No zinc/violet residue: `grep -E "text-zinc|bg-zinc|text-violet|hover:text-violet" src/components/layout/Header.tsx` returns 0 lines
2. Brand present: `grep "Tommy Lahitte" src/components/layout/Header.tsx` matches
3. Correct nav links: `grep -E 'href="/blog"|href="/projects"|href="/about"' src/components/layout/Header.tsx` returns 3 lines
4. Scroll threshold: `grep "scrollY > 48" src/components/layout/Header.tsx` matches
5. Build + lint: `npm run build && npm run lint` exits 0
</verification>

<success_criteria>
- Header is a client component with scroll state management
- Brand "Tommy Lahitte" visible at left on all viewport widths
- Navigation shows Blog, Projects, About (in order) at right, no hamburger
- Background transparent at top, bg-surface with border-border at 48px+ scroll
- Zero zinc or violet Tailwind classes in the file
- Build and lint pass
</success_criteria>

<output>
After completion, create `.planning/phases/01-design-system/01-02-SUMMARY.md`
</output>
