---
phase: 01-design-system
plan: 03
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - src/components/layout/Footer.tsx
  - src/components/ui/Tag.tsx
  - src/components/ui/CodeBlock.tsx
  - src/components/tips/CategoryBadge.tsx
  - src/components/home/Hero.tsx
autonomous: true
requirements:
  - DSGN-02

must_haves:
  truths:
    - "Footer uses semantic tokens — no zinc or violet classes remain"
    - "Tag component uses semantic tokens — bg-surface-raised, text-text-muted, border-border"
    - "CodeBlock outer border uses border-border; dark interior kept (industry convention)"
    - "CategoryBadge per-badge colors use warm accent-adjacent palette instead of violet/teal"
    - "Hero Tailwind color classes migrated to semantic tokens; conic-gradient halo uses accent color"
    - "grep audit of all 5 files returns zero zinc/violet hardcoded classes"
  artifacts:
    - path: "src/components/layout/Footer.tsx"
      provides: "Footer with semantic token classes"
      contains: "text-text-muted"
    - path: "src/components/ui/Tag.tsx"
      provides: "Tag with semantic token classes"
      contains: "bg-surface-raised"
    - path: "src/components/ui/CodeBlock.tsx"
      provides: "CodeBlock with token outer border, dark interior preserved"
      contains: "border-border"
    - path: "src/components/tips/CategoryBadge.tsx"
      provides: "CategoryBadge with warm accent-adjacent badge colors"
      contains: "accent"
    - path: "src/components/home/Hero.tsx"
      provides: "Hero with semantic token classes; conic-gradient uses accent"
      contains: "ring-accent"
  key_links:
    - from: "Tag.tsx"
      to: "tailwind.config.ts tokens"
      via: "bg-surface-raised class"
      pattern: "bg-surface-raised"
    - from: "Hero.tsx conic-gradient"
      to: "accent palette"
      via: "rgba(200,90,58,...) in inline style"
      pattern: "200,90,58"
---

<objective>
Token migration of 5 existing components: Footer, Tag, CodeBlock, CategoryBadge, Hero. Replace all hardcoded zinc/violet Tailwind classes with semantic tokens. No structural changes — pure color class substitution (plus CategoryBadge warm palette update and Hero conic-gradient color swap).

Purpose: After this plan, a grep for zinc/violet classes across the entire src/ directory should return zero results from these 5 files. Combined with Plan 02 (Header) and Plan 04 (BlogCard), the full palette migration is complete.
Output: 5 migrated component files, zero zinc/violet residue.
</objective>

<execution_context>
@/Users/tommy.lahitte/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tommy.lahitte/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-design-system/01-UI-SPEC.md
@.planning/phases/01-design-system/01-RESEARCH.md
@.planning/phases/01-design-system/01-01-SUMMARY.md

<interfaces>
<!-- Token reference — established in Plan 01 -->
<!-- CSS vars in globals.css :root: -->
<!--   --surface: #FAFAF7 | --surface-raised: #F0EBE0 | --text-primary: #1A1816 -->
<!--   --text-muted: #7A6F68 | --accent: #C85A3A | --accent-hover: #A8432A | --border: #E2D9CE -->

<!-- Tailwind class equivalents: -->
<!--   bg-surface | bg-surface-raised | text-text-primary | text-text-muted -->
<!--   text-accent | hover:text-accent | border-border | ring-accent -->
<!--   bg-accent | hover:bg-accent-hover -->

<!-- UI-SPEC Component Migration Contract: -->
<!-- Footer:        bg-zinc-950 → bg-surface | text-zinc-500 → text-text-muted | hover:text-violet-400 → hover:text-accent | border-zinc-800 → border-border -->
<!-- Tag:           bg-zinc-800 → bg-surface-raised | text-zinc-300 → text-text-muted | border-zinc-700 → border-border -->
<!-- CodeBlock:     border-zinc-800 (outer) → border-border; KEEP dark interior (bg-zinc-900, bg-zinc-800, text-zinc-200, text-zinc-400) -->
<!-- CategoryBadge: Update inline badge colors to warm accent-adjacent palette (see action below) -->
<!-- Hero:          ring-violet-500 → ring-accent | conic-gradient violet/cyan → accent rgba | border-zinc-700 → border-border | text-zinc-* → semantic tokens | bg-violet-* → bg-accent -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate Footer, Tag, and CodeBlock to semantic tokens</name>
  <files>src/components/layout/Footer.tsx, src/components/ui/Tag.tsx, src/components/ui/CodeBlock.tsx</files>
  <read_first>
    - src/components/layout/Footer.tsx — current state (zinc-950 bg, zinc-500 icon/text colors, violet-400 hover, zinc-800 border-t)
    - src/components/ui/Tag.tsx — current state (zinc-800 bg, zinc-300 text, zinc-700 border)
    - src/components/ui/CodeBlock.tsx — current state (zinc-800 outer, zinc-900 pre, zinc-200 code text — dark interior stays)
  </read_first>
  <action>
**Footer.tsx** — substitute all zinc/violet class names. No structural changes to the JSX, no SVG changes, no href changes. Replace only class string values:

```
border-zinc-800   → border-border        (footer border-t)
bg-zinc-950       → bg-surface           (footer background)
text-zinc-500     → text-text-muted      (icon link color — all 7 icon links)
hover:text-violet-400 → hover:text-accent  (icon link hover — all 7 icon links)
text-sm text-zinc-500 → text-sm text-text-muted  (copyright paragraph)
```

The footer's copyright `<p>` tag: change `className="text-center text-sm text-zinc-500"` to `className="text-center text-sm text-text-muted"`.

---

**Tag.tsx** — replace all 3 color classes in the single `<span>`:

```
bg-zinc-800   → bg-surface-raised
text-zinc-300 → text-text-muted
border-zinc-700 → border-border
```

Full resulting span:
```tsx
<span className="inline-block rounded px-2 py-0.5 text-xs font-medium bg-surface-raised text-text-muted border border-border">
  {label}
</span>
```

---

**CodeBlock.tsx** — partial migration: update ONLY the outer wrapper border and the language tab. Keep the `<pre>` and `<code>` dark interior classes exactly as-is (dark code blocks on light pages are industry convention — intentional contrast).

Changes:
```
div wrapper:    border-zinc-800 → border-border
language tab div: bg-zinc-800   → bg-surface-raised
                  text-zinc-400 → text-text-muted
                  border-b border-zinc-700 → border-b border-border
```

The `<pre className="bg-zinc-900 ...">` and `<code className="... text-zinc-200 ...">` lines are NOT changed — they stay dark.

Full resulting component after changes:
```tsx
interface CodeBlockProps {
  code: string
  language?: string
}

export default function CodeBlock({ code, language }: CodeBlockProps) {
  return (
    <div className="my-4 rounded-lg overflow-hidden border border-border">
      {language && (
        <div className="bg-surface-raised px-4 py-1.5 text-xs font-mono text-text-muted border-b border-border">
          {language}
        </div>
      )}
      <pre className="bg-zinc-900 p-4 overflow-x-auto">
        <code className="text-sm font-mono text-zinc-200 whitespace-pre">{code}</code>
      </pre>
    </div>
  )
}
```
  </action>
  <verify>
    <automated>grep -E "text-zinc|bg-zinc|text-violet|hover:text-violet|border-zinc" /Users/tommy.lahitte/Documents/Github/tommy-lahitte-debug-vault/src/components/layout/Footer.tsx</automated>
    <automated>grep "bg-surface-raised text-text-muted border border-border" /Users/tommy.lahitte/Documents/Github/tommy-lahitte-debug-vault/src/components/ui/Tag.tsx</automated>
    <automated>grep "border border-border" /Users/tommy.lahitte/Documents/Github/tommy-lahitte-debug-vault/src/components/ui/CodeBlock.tsx</automated>
    <automated>grep "bg-zinc-900" /Users/tommy.lahitte/Documents/Github/tommy-lahitte-debug-vault/src/components/ui/CodeBlock.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Footer.tsx contains `bg-surface` on the footer element
    - Footer.tsx contains `text-text-muted` on icon link elements and copyright paragraph
    - Footer.tsx contains `hover:text-accent` on all icon link hover states (7 occurrences)
    - Footer.tsx contains `border-border` on the border-t
    - Footer.tsx does NOT contain `text-zinc-`, `bg-zinc-`, `hover:text-violet-`, `border-zinc-`
    - Tag.tsx contains exactly `bg-surface-raised text-text-muted border border-border`
    - Tag.tsx does NOT contain `bg-zinc-800`, `text-zinc-300`, or `border-zinc-700`
    - CodeBlock.tsx outer `<div>` contains `border border-border`
    - CodeBlock.tsx language tab `<div>` contains `bg-surface-raised` and `text-text-muted` and `border-border`
    - CodeBlock.tsx `<pre>` still contains `bg-zinc-900` (dark interior preserved)
    - CodeBlock.tsx `<code>` still contains `text-zinc-200` (dark interior preserved)
  </acceptance_criteria>
  <done>Footer, Tag, CodeBlock use semantic tokens. Dark code interior preserved. Zero zinc/violet classes in Footer and Tag.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate CategoryBadge and Hero to kraft palette</name>
  <files>src/components/tips/CategoryBadge.tsx, src/components/home/Hero.tsx</files>
  <read_first>
    - src/components/tips/CategoryBadge.tsx — current state (uses blue-500/red-500/green-500 Tailwind opacity classes and border classes; NO violet or zinc — verify before changing; the current classes are NOT zinc/violet so minimal change needed — see action for what actually needs updating)
    - src/components/home/Hero.tsx — current state (violet-400/violet-300/violet-500/violet-600/violet-950 + zinc-100/zinc-400/zinc-500/zinc-700 classes, conic-gradient with violet/cyan rgba inline style, ring-violet-500)
  </read_first>
  <action>
**CategoryBadge.tsx** — The current implementation uses `blue-500/10`, `red-500/10`, `green-500/10` opacity classes (NOT zinc or violet — these are semantic category colors). Per RESEARCH.md §Pitfall 5 and UI-SPEC §Accent reserved list, update to use warm accent-adjacent colors. Replace the `categoryConfig` object:

```tsx
const categoryConfig: Record<Category, { label: string; className: string }> = {
  editor: {
    label: 'Editor',
    className: 'bg-accent/10 text-accent border-accent/30',
  },
  debugging: {
    label: 'Debugging',
    className: 'bg-accent/15 text-accent-hover border-accent/40',
  },
  'qa-workflow': {
    label: 'QA Workflow',
    className: 'bg-surface-raised text-text-muted border-border',
  },
}
```

The `<span>` element and all its other classes remain unchanged — only the `categoryConfig` values change.

---

**Hero.tsx** — Token migration of all color classes. No structural changes: the two-column layout (text left, portrait right), CTAs, SVG icons, and Image component all remain identical. Only class strings and the conic-gradient inline style change.

**Hero.tsx color class replacements:**

Heading text:
```
text-zinc-100 font-display  → text-text-primary font-display   (h1 class)
```

Subtitle:
```
text-violet-400             → text-accent                       ("Senior QA Engineer" span)
text-zinc-500               → text-text-muted                   (separator "|" span)
text-violet-300             → text-accent                       (italic "Unreal Engine Expert" em)
```

Rule:
```
border-violet-500/50        → border-border                     (hr)
```

Description text:
```
text-zinc-400               → text-text-muted                   (paragraph)
```

Avatar ring:
```
ring-violet-500             → ring-accent                       (Image className)
```

Primary CTA (Unreal Tips button):
```
bg-violet-600               → bg-accent                         (button background)
hover:bg-violet-500         → hover:bg-accent-hover             (button hover)
```
Remove `hover:scale-[1.03] active:scale-95` from primary CTA if present (Framer Motion handles hover on cards; plain button scale is fine to keep — Claude's discretion, keeping it is acceptable).

Secondary CTAs (Read Blog, GitHub buttons):
```
border-zinc-700             → border-border
text-zinc-300               → text-text-muted
hover:border-violet-500     → hover:border-accent
hover:bg-violet-950/50      → hover:bg-surface-raised
hover:text-violet-300       → hover:text-accent
```

Conic-gradient halo inline style (the rotating animated div):
Replace:
```
background: 'conic-gradient(from 0deg, transparent 0%, transparent 45%, rgba(124,58,237,0.7) 62%, rgba(6,182,212,0.85) 78%, rgba(167,139,250,0.9) 90%, transparent 100%)'
```
With:
```
background: 'conic-gradient(from 0deg, transparent 0%, transparent 45%, rgba(200,90,58,0.7) 62%, rgba(168,67,42,0.85) 78%, rgba(200,90,58,0.9) 90%, transparent 100%)'
```
(Uses accent #C85A3A = rgba(200,90,58) and accent-hover #A8432A = rgba(168,67,42) — warm rust tones replace the violet/cyan)

Keep `animate-spin-slow` class unchanged (it uses the `spin-slow` keyframe from tailwind.config.ts which was preserved in Plan 01).
  </action>
  <verify>
    <automated>grep -E "text-violet|bg-violet|hover:text-violet|ring-violet|border-violet|hover:border-violet" /Users/tommy.lahitte/Documents/Github/tommy-lahitte-debug-vault/src/components/home/Hero.tsx</automated>
    <automated>grep -E "text-zinc|bg-zinc|border-zinc" /Users/tommy.lahitte/Documents/Github/tommy-lahitte-debug-vault/src/components/home/Hero.tsx</automated>
    <automated>grep "ring-accent" /Users/tommy.lahitte/Documents/Github/tommy-lahitte-debug-vault/src/components/home/Hero.tsx</automated>
    <automated>grep "200,90,58" /Users/tommy.lahitte/Documents/Github/tommy-lahitte-debug-vault/src/components/home/Hero.tsx</automated>
    <automated>grep "bg-accent/10" /Users/tommy.lahitte/Documents/Github/tommy-lahitte-debug-vault/src/components/tips/CategoryBadge.tsx</automated>
    <automated>cd /Users/tommy.lahitte/Documents/Github/tommy-lahitte-debug-vault && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Hero.tsx contains `ring-accent` on the Image element (ring-violet-500 replaced)
    - Hero.tsx contains `bg-accent` on the primary CTA button (bg-violet-600 replaced)
    - Hero.tsx contains `hover:bg-accent-hover` on the primary CTA button (hover:bg-violet-500 replaced)
    - Hero.tsx contains `text-accent` where text-violet-400 and text-violet-300 were
    - Hero.tsx contains `rgba(200,90,58` in the conic-gradient inline style (violet replaced with rust)
    - Hero.tsx does NOT contain `text-violet-`, `bg-violet-`, `ring-violet-`, `border-violet-`, `hover:text-violet-`, `hover:bg-violet-`
    - Hero.tsx does NOT contain `text-zinc-`, `bg-zinc-`, `border-zinc-` (except possibly `bg-zinc-` in comments — no classes)
    - CategoryBadge.tsx categoryConfig contains `bg-accent/10` for the editor category
    - CategoryBadge.tsx categoryConfig contains `bg-surface-raised` for the qa-workflow category
    - CategoryBadge.tsx does NOT contain `bg-blue-`, `bg-red-`, `bg-green-` (old category colors replaced)
    - `npm run build` exits 0
    - `npm run lint` exits 0
  </acceptance_criteria>
  <done>Hero uses kraft palette tokens; conic-gradient halo is warm rust. CategoryBadge uses accent-adjacent warm palette. Zero violet/zinc residue in both files.</done>
</task>

</tasks>

<verification>
After both tasks complete, run full zinc/violet audit across all 5 files:

```bash
grep -rE "text-zinc|bg-zinc|border-zinc|text-violet|bg-violet|ring-violet|border-violet|hover:text-violet|hover:bg-violet|hover:border-violet" \
  src/components/layout/Footer.tsx \
  src/components/ui/Tag.tsx \
  src/components/tips/CategoryBadge.tsx \
  src/components/home/Hero.tsx
```

Expected: 0 results (CodeBlock is excluded — its dark interior bg-zinc-900/text-zinc-200 are intentionally kept).

Build + lint: `npm run build && npm run lint` both exit 0.
</verification>

<success_criteria>
- Footer, Tag, CategoryBadge, Hero: zero zinc/violet Tailwind classes
- CodeBlock: outer border uses border-border; dark pre/code interior preserved
- Hero conic-gradient uses rgba(200,90,58) rust tones instead of violet/cyan
- CategoryBadge uses accent-adjacent warm palette (accent, accent-hover, surface-raised)
- npm run build and npm run lint pass
</success_criteria>

<output>
After completion, create `.planning/phases/01-design-system/01-03-SUMMARY.md`
</output>
