---
phase: 02-notion-foundation-blog
plan: "01"
type: execute
wave: 0
depends_on: []
files_modified:
  - vitest.config.ts
  - src/lib/notion.test.ts
  - src/lib/blog.test.ts
  - src/components/blog/NotionBlock.test.tsx
autonomous: true
requirements:
  - NOTI-01
  - NOTI-02
  - NOTI-03
  - NOTI-04
  - BLOG-02

must_haves:
  truths:
    - "Running `npx vitest run` exits 0 with 0 test failures (all tests are stubs passing with `todo`)"
    - "vitest.config.ts exists and is configured for Next.js + jsdom environment"
    - "Test files cover the 4 requirement areas: notion client, blog DAL, block renderer, slug preservation"
  artifacts:
    - path: "vitest.config.ts"
      provides: "Test runner config wiring React + jsdom"
      contains: "defineConfig"
    - path: "src/lib/notion.test.ts"
      provides: "Stubs for NOTI-01 (pagination, image URL mapping)"
      contains: "getAllPosts"
    - path: "src/lib/blog.test.ts"
      provides: "Stubs for NOTI-04 (property mapping) and BLOG-02 (slug preservation)"
      contains: "mapNotionPageToBlogPost"
    - path: "src/components/blog/NotionBlock.test.tsx"
      provides: "Stubs for NOTI-02 (9 block types) and NOTI-03 (silent fallback)"
      contains: "renderBlock"
  key_links:
    - from: "vitest.config.ts"
      to: "src/**/*.test.{ts,tsx}"
      via: "include glob"
      pattern: "include.*test"
---

<objective>
Install vitest and create failing-stub test files for all testable Phase 2 requirements.

Purpose: Satisfy the Nyquist rule — every code-producing task in later waves must have a test file to verify against before it is written. Wave 0 creates the scaffold so that Wave 1 and Wave 2 tasks can run `npx vitest run` after each commit.
Output: vitest.config.ts, src/lib/notion.test.ts, src/lib/blog.test.ts, src/components/blog/NotionBlock.test.tsx — all passing (stubs with `it.todo()`).
</objective>

<execution_context>
@/Users/tommy.lahitte/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tommy.lahitte/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-notion-foundation-blog/02-RESEARCH.md
@.planning/phases/02-notion-foundation-blog/02-VALIDATION.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Install vitest and create vitest.config.ts</name>
  <files>vitest.config.ts, package.json</files>
  <read_first>
    - package.json (check existing devDependencies and scripts before adding)
  </read_first>
  <action>
    Run:
    ```
    npm install -D vitest @vitejs/plugin-react jsdom @testing-library/react @testing-library/jest-dom
    ```

    Create `vitest.config.ts` at the repo root:

    ```typescript
    import { defineConfig } from 'vitest/config'
    import react from '@vitejs/plugin-react'
    import path from 'path'

    export default defineConfig({
      plugins: [react()],
      test: {
        environment: 'jsdom',
        setupFiles: [],
        globals: true,
        include: ['src/**/*.test.{ts,tsx}'],
      },
      resolve: {
        alias: {
          '@': path.resolve(__dirname, 'src'),
        },
      },
    })
    ```

    Do NOT add a `test` script to package.json unless one is absent — check first. If `"test"` already exists, do not overwrite it. The executor will invoke vitest via `npx vitest run` throughout the phase.
  </action>
  <verify>
    <automated>npx vitest run --reporter=verbose 2>&1 | grep -E "no test files found|passed|failed" || echo "vitest not installed correctly"</automated>
  </verify>
  <acceptance_criteria>
    - vitest.config.ts exists at repo root
    - vitest.config.ts contains `defineConfig`
    - vitest.config.ts contains `environment: 'jsdom'`
    - vitest.config.ts contains `alias: { '@': path.resolve(__dirname, 'src') }`
    - package.json devDependencies contains `"vitest"`
    - package.json devDependencies contains `"@vitejs/plugin-react"`
    - package.json devDependencies contains `"jsdom"`
    - `npx vitest run` exits without "Cannot find module 'vitest'" error
  </acceptance_criteria>
  <done>vitest is installed and configured. Running `npx vitest run` with no test files exits 0 or prints "No test files found".</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Create stub test files for Phase 2 requirements</name>
  <files>
    src/lib/notion.test.ts,
    src/lib/blog.test.ts,
    src/components/blog/NotionBlock.test.tsx
  </files>
  <read_first>
    - .planning/phases/02-notion-foundation-blog/02-RESEARCH.md (test map: §Validation Architecture → Phase Requirements → Test Map)
    - .planning/phases/02-notion-foundation-blog/02-VALIDATION.md (Wave 0 Requirements section)
  </read_first>
  <action>
    Create three stub files. Each uses `it.todo()` so vitest passes with zero failures. The describe block names and todo descriptions match the requirement IDs they will verify — this makes it easy to fill in the real assertions in later waves.

    **`src/lib/notion.test.ts`** — covers NOTI-01 (pagination, image URL mapping):
    ```typescript
    import { describe, it, expect, vi } from 'vitest'

    describe('notion data layer', () => {
      describe('getAllPosts (NOTI-01)', () => {
        it.todo('returns an array of BlogPost objects mapped from Notion pages')
        it.todo('fetches all pages when has_more is true (pagination cursor loop)')
        it.todo('returns local /notion-images/ paths, never raw Notion file URLs, for cover images')
        it.todo('filters to Status === "Published" only')
        it.todo('sorts posts by Date descending')
      })

      describe('getPostBySlug (NOTI-01)', () => {
        it.todo('returns the correct BlogPost for a matching slug')
        it.todo('returns undefined for an unknown slug')
        it.todo('fetches block children recursively (has_children toggle blocks)')
      })
    })
    ```

    **`src/lib/blog.test.ts`** — covers NOTI-04 (property mapping) and BLOG-02 (slug preservation):
    ```typescript
    import { describe, it, expect } from 'vitest'

    describe('mapNotionPageToBlogPost (NOTI-04)', () => {
      it.todo('extracts slug from Slug rich_text property')
      it.todo('extracts title from Title title property')
      it.todo('extracts date from Date date property')
      it.todo('extracts excerpt from Excerpt rich_text property')
      it.todo('maps external cover image URL directly (type: external)')
      it.todo('returns undefined image for file-type cover (handled separately by image downloader)')
    })

    describe('slug preservation (BLOG-02)', () => {
      it.todo('"elevenapps-immersive-show-swift" slug is preserved in getAllPosts output')
      it.todo('"technical-director-people-person-show" slug is preserved in getAllPosts output')
    })
    ```

    **`src/components/blog/NotionBlock.test.tsx`** — covers NOTI-02 (9 block types) and NOTI-03 (silent fallback):
    ```typescript
    import { describe, it } from 'vitest'

    describe('NotionBlock renderer (NOTI-02)', () => {
      it.todo('renders paragraph block as <p> with text-text-primary leading-relaxed')
      it.todo('renders heading_1 as <h1> with text-3xl font-semibold mt-10 mb-3')
      it.todo('renders heading_2 as <h2> with text-lg font-semibold mt-8 mb-2')
      it.todo('renders heading_3 as <h3> with text-base font-semibold mt-6 mb-1')
      it.todo('renders code block using existing CodeBlock component with bg-zinc-900')
      it.todo('renders callout with bg-surface-raised border border-border rounded-lg px-4 py-3')
      it.todo('renders bulleted_list_item as <li> inside <ul class="list-disc list-outside ml-5 space-y-1">')
      it.todo('renders numbered_list_item as <li> inside <ol class="list-decimal list-outside ml-5 space-y-1">')
      it.todo('renders image block with w-full rounded-xl and optional caption in text-xs text-text-muted')
      it.todo('renders toggle as <details>/<summary> — summary has font-medium text-text-primary cursor-pointer')
      it.todo('renders divider as <hr class="border-t border-border my-8">')
    })

    describe('NotionBlock silent fallback (NOTI-03)', () => {
      it.todo('renders null for an unknown block type without throwing')
      it.todo('renders null for an undefined block without throwing')
    })
    ```
  </action>
  <verify>
    <automated>npx vitest run --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - src/lib/notion.test.ts exists
    - src/lib/blog.test.ts exists
    - src/components/blog/NotionBlock.test.tsx exists
    - `npx vitest run` exits 0
    - `npx vitest run` output contains "0 failed"
    - `npx vitest run` output does NOT contain "Error" or "Cannot find module"
    - src/lib/notion.test.ts contains `describe('getAllPosts (NOTI-01)'`
    - src/lib/blog.test.ts contains `describe('mapNotionPageToBlogPost (NOTI-04)'`
    - src/components/blog/NotionBlock.test.tsx contains `describe('NotionBlock renderer (NOTI-02)'`
    - src/components/blog/NotionBlock.test.tsx contains `describe('NotionBlock silent fallback (NOTI-03)'`
  </acceptance_criteria>
  <done>All three stub test files exist. `npx vitest run` passes with 0 failures (all tests are marked todo). Wave 1 tasks can now run the suite as a health check after each commit.</done>
</task>

</tasks>

<verification>
- `npx vitest run --reporter=verbose` exits 0
- Output shows "0 failed" and a list of todo test names grouped by describe block
- vitest.config.ts `@` alias resolves correctly (no "Cannot find module '@/lib/...'" error)
</verification>

<success_criteria>
Wave 0 is complete when `npx vitest run` exits 0 with the three test files loaded, all tests in `todo` state, and zero failures.
</success_criteria>

<output>
After completion, create `.planning/phases/02-notion-foundation-blog/02-01-SUMMARY.md`
</output>
