---
phase: 04-homepage-about
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/page.tsx
  - src/components/home/Hero.tsx
autonomous: true
requirements:
  - HOME-01
  - HOME-02
  - HOME-03
  - HOME-04

must_haves:
  truths:
    - "A first-time visitor understands Tommy is a maker/tinkerer within 5 seconds of landing"
    - "The homepage shows 2-3 recent projects with visual preview cards linking to /blog/{slug}"
    - "The homepage shows 2-3 recent articles (category Article) in a separate section"
    - "The homepage embeds the AboutCard component as an inline short bio section"
    - "The hero CTA primary button links to /blog?category=Project, not /tips"
    - "Homepage metadata title and description reflect maker identity, not QA Engineer lede"
  artifacts:
    - path: "src/app/page.tsx"
      provides: "Category-filtered homepage with 4 sections (hero, projects, articles, about)"
      contains: "filter.*category.*Project"
    - path: "src/components/home/Hero.tsx"
      provides: "Maker-identity hero copy and updated CTAs"
      contains: "See Projects"
  key_links:
    - from: "src/app/page.tsx"
      to: "src/lib/blog.ts"
      via: "getAllPosts() import and call"
      pattern: "getAllPosts"
    - from: "src/app/page.tsx"
      to: "src/components/home/AboutCard.tsx"
      via: "AboutCard import and render in section 4"
      pattern: "import AboutCard"
    - from: "src/components/home/Hero.tsx"
      to: "/blog?category=Project"
      via: "primary CTA href"
      pattern: "blog\\?category=Project"
---

<objective>
Rewrite the homepage to communicate Tommy's maker identity and showcase real Notion-sourced content by category.

Purpose: The homepage currently positions Tommy as "QA Engineer & Unreal Debug Vault" with a tips-first content hierarchy. Phase 4 shifts it to "maker & tinkerer" with projects and articles front and center. All building blocks exist from Phases 1-3 — this is pure composition and copy work.

Output: Updated `src/app/page.tsx` (4-section layout with category-filtered Notion data) and `src/components/home/Hero.tsx` (maker copy, updated CTAs).
</objective>

<execution_context>
@/Users/tommy.lahitte/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tommy.lahitte/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-homepage-about/04-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. -->

From src/lib/blog-types.ts:
```typescript
export type PostCategory = 'Project' | 'Article' | 'Recommendation'
export type ProjectStatus = 'In Progress' | 'Complete' | 'Archived'

export interface BlogPost {
  slug: string
  title: string
  date: string
  excerpt: string
  image?: string
  imageAlt?: string
  content: BlogBlock[]
  link?: { label: string; url: string }
  category?: PostCategory
  status?: ProjectStatus
  gallery?: string[]
}
```

From src/lib/blog.ts:
```typescript
export async function getAllPosts(): Promise<BlogPost[]>
// Returns posts sorted descending by Date from Notion query. No local sort needed.
```

From src/components/blog/BlogCard.tsx:
```typescript
interface BlogCardProps { post: BlogPost }
export default function BlogCard({ post }: BlogCardProps)
// 'use client' component with motion.article, h-52 image, date, title, excerpt, "Read more"
```

From src/components/home/AboutCard.tsx:
```typescript
export default function AboutCard()
// 'use client' component — self-contained, no props needed
```

From src/components/home/Hero.tsx:
```typescript
export default function Hero()
// 'use client' component with motion, TextShimmer, avatar image, CTA buttons
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite Hero.tsx — maker identity copy and updated CTAs</name>
  <files>src/components/home/Hero.tsx</files>
  <read_first>
    - src/components/home/Hero.tsx
    - .planning/phases/04-homepage-about/04-RESEARCH.md
  </read_first>
  <action>
Rewrite the text content and CTA links in Hero.tsx. Do NOT change the component structure, avatar, animation, or styling — only the text strings and CTA hrefs/labels.

**Tagline (the TextShimmer line):**
Replace:
```
<TextShimmer ...>Senior QA Engineer</TextShimmer>
<span className="text-text-muted mx-2">·</span>
<span className="text-text-muted">Epic Games</span>
```
With:
```
<TextShimmer ...>Maker &amp; Tinkerer</TextShimmer>
<span className="text-text-muted mx-2">·</span>
<span className="text-text-muted">Art meets Technology</span>
```

**Descriptive paragraph:**
Replace:
```
On this website you'll find engineering field notes, side projects, and the things I think are worth sharing. Welcome aboard.
```
With:
```
I bridge art and technology — building projects at the intersection of electronics, software, and creative experimentation. Senior QA Engineer at Epic Games by day, maker by nature.
```
(Keep the `<br className="hidden sm:block" />` between the two sentences.)

**CTA buttons — update hrefs and labels (keep all existing Tailwind classes and SVG icons):**
1. First button (currently "Unreal Tips" → `/tips`): Change to label `See Projects`, href `/blog?category=Project`
2. Second button (currently "Read Journal" → `/blog`): Keep label `Read Journal`, keep href `/blog`
3. Third button (GitHub external link): Keep unchanged

No structural, styling, or animation changes. Only text content and href values.
  </action>
  <verify>
    <automated>grep -n "See Projects" src/components/home/Hero.tsx && grep -n "blog?category=Project" src/components/home/Hero.tsx && grep -n "Maker" src/components/home/Hero.tsx && grep -n "Read Journal" src/components/home/Hero.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Hero.tsx contains the string "Maker &amp; Tinkerer" (or "Maker & Tinkerer" in JSX) inside the TextShimmer component
    - Hero.tsx contains "Art meets Technology" as a text-text-muted span
    - Hero.tsx contains href="/blog?category=Project" on the first CTA button
    - Hero.tsx contains the label "See Projects" on the first CTA button
    - Hero.tsx contains "Senior QA Engineer at Epic Games by day" in the descriptive paragraph
    - Hero.tsx still contains href="/blog" on the second CTA (Read Journal)
    - Hero.tsx still contains href="https://github.com/tlahitte" on the third CTA
    - Hero.tsx still has 'use client' as first line
    - Hero.tsx still imports motion from 'motion/react'
    - No structural/styling changes — only text and href values changed
  </acceptance_criteria>
  <done>Hero communicates maker identity first (not QA Engineer as lede), CTAs point to projects and journal, Epic Games appears as credibility signal in body text not headline</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite page.tsx — category-filtered sections, metadata, remove tips</name>
  <files>src/app/page.tsx</files>
  <read_first>
    - src/app/page.tsx
    - src/components/home/Hero.tsx
    - src/components/blog/BlogCard.tsx
    - src/components/home/AboutCard.tsx
    - src/lib/blog-types.ts
    - src/lib/blog.ts
    - .planning/phases/04-homepage-about/04-RESEARCH.md
  </read_first>
  <action>
Rewrite `src/app/page.tsx` completely. The new page has 4 sections and updated metadata.

**Imports — replace:**
```typescript
import type { Metadata } from 'next'
import Link from 'next/link'
import Hero from '@/components/home/Hero'
import BlogCard from '@/components/blog/BlogCard'
import AboutCard from '@/components/home/AboutCard'
import { getAllPosts } from '@/lib/blog'
```
Remove the `TipCard` import and the `getAllTips` import entirely. Add `AboutCard` import.

**Metadata — replace entirely:**
```typescript
export const metadata: Metadata = {
  title: 'Tommy Lahitte | Maker & Tinkerer',
  description:
    'Tommy Lahitte — maker who bridges art and technology. Projects, field notes, and things worth building.',
  alternates: {
    canonical: 'https://tommylahitte.com/',
  },
  openGraph: {
    title: 'Tommy Lahitte | Maker & Tinkerer',
    description:
      'Tommy Lahitte — maker who bridges art and technology. Projects, field notes, and things worth building.',
    url: 'https://tommylahitte.com/',
    type: 'website',
  },
}
```

**Component body — replace entirely:**
```typescript
export default async function HomePage() {
  const allPosts = await getAllPosts()

  const featuredProjects = allPosts
    .filter((p) => p.category === 'Project')
    .slice(0, 3)

  const recentArticles = allPosts
    .filter((p) => p.category === 'Article')
    .slice(0, 3)

  return (
    <>
      {/* Section 1: Hero */}
      <section
        className="bg-gradient-to-b from-surface-raised to-surface border-b border-border relative overflow-hidden hero-texture"
        style={{
          background: 'radial-gradient(ellipse 80% 60% at 50% 0%, rgba(200,90,58,0.06) 0%, transparent 70%), linear-gradient(to bottom, var(--surface-raised), var(--surface))',
        }}
      >
        <div className="mx-auto max-w-5xl px-4 sm:px-6">
          <div className="flex flex-col gap-6 py-10 sm:py-16">
            <Hero />
          </div>
        </div>
      </section>

      {/* Section 2: Featured Projects — HOME-02 */}
      {featuredProjects.length > 0 && (
        <section className="pt-12 sm:pt-16 pb-12 bg-surface">
          <div className="mx-auto max-w-5xl px-4 sm:px-6">
            <h2 className="text-2xl font-bold text-text-primary mb-8 font-display">Projects</h2>
            <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-4">
              {featuredProjects.map((post) => (
                <BlogCard key={post.slug} post={post} />
              ))}
            </div>
            <div className="mt-8 text-center">
              <Link
                href="/blog?category=Project"
                className="inline-flex items-center gap-1.5 text-sm font-medium text-accent hover:text-accent-hover transition-colors"
              >
                View All Projects
                <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2" strokeLinecap="round" strokeLinejoin="round" aria-hidden="true"><path d="M5 12h14M12 5l7 7-7 7" /></svg>
              </Link>
            </div>
          </div>
        </section>
      )}

      {/* Section 3: Recent Articles — HOME-03 */}
      {recentArticles.length > 0 && (
        <section className="pt-12 sm:pt-16 pb-12 bg-surface-raised">
          <div className="mx-auto max-w-5xl px-4 sm:px-6">
            <h2 className="text-2xl font-bold text-text-primary mb-8 font-display">Journal</h2>
            <div className="grid grid-cols-1 sm:grid-cols-2 gap-4">
              {recentArticles.map((post) => (
                <BlogCard key={post.slug} post={post} />
              ))}
            </div>
            <div className="mt-8 text-center">
              <Link
                href="/blog"
                className="inline-flex items-center gap-1.5 text-sm font-medium text-accent hover:text-accent-hover transition-colors"
              >
                View All Articles
                <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2" strokeLinecap="round" strokeLinejoin="round" aria-hidden="true"><path d="M5 12h14M12 5l7 7-7 7" /></svg>
              </Link>
            </div>
          </div>
        </section>
      )}

      {/* Section 4: Short About — HOME-04 */}
      <section className="pt-12 sm:pt-16 pb-32 bg-surface">
        <div className="mx-auto max-w-5xl px-4 sm:px-6">
          <AboutCard />
        </div>
      </section>
    </>
  )
}
```

Key points:
- Call `getAllPosts()` exactly ONCE, filter in memory
- Guard project and article sections with `.length > 0` (graceful degradation if no categorized content)
- Remove "Latest Tips" section and `getAllTips` import entirely
- Remove `TipCard` import entirely
- "View All Projects" links to `/blog?category=Project`
- "View All Articles" links to `/blog`
- AboutCard in section 4 satisfies HOME-04
  </action>
  <verify>
    <automated>grep -n "getAllTips\|TipCard\|Latest Tips" src/app/page.tsx; echo "---"; grep -n "Maker" src/app/page.tsx && grep -n "featuredProjects" src/app/page.tsx && grep -n "recentArticles" src/app/page.tsx && grep -n "AboutCard" src/app/page.tsx && grep -n "category.*Project" src/app/page.tsx</automated>
  </verify>
  <acceptance_criteria>
    - page.tsx does NOT contain "getAllTips" or "TipCard" or "Latest Tips" or "@/lib/tips" imports
    - page.tsx contains `import AboutCard from '@/components/home/AboutCard'`
    - page.tsx metadata title contains "Maker & Tinkerer" (not "QA Engineer & Unreal Debug Vault")
    - page.tsx metadata description contains "maker who bridges art and technology"
    - page.tsx calls `getAllPosts()` exactly once (single `await getAllPosts()` call)
    - page.tsx contains `filter((p) => p.category === 'Project')` for featured projects
    - page.tsx contains `filter((p) => p.category === 'Article')` for recent articles
    - page.tsx contains `.slice(0, 3)` after both filters
    - page.tsx contains `featuredProjects.length > 0` guard for projects section
    - page.tsx contains `recentArticles.length > 0` guard for articles section
    - page.tsx renders `<AboutCard />` in the fourth section
    - page.tsx contains `href="/blog?category=Project"` for the View All Projects link
    - pnpm vitest run exits 0
    - pnpm run build exits 0 (static export validates all pages)
  </acceptance_criteria>
  <done>Homepage has 4 sections (hero, projects, articles, about), metadata reflects maker identity, tips section removed, all content sourced from Notion via getAllPosts() with category filtering</done>
</task>

</tasks>

<verification>
- `pnpm vitest run` — all existing tests pass (no regressions)
- `pnpm run build` — static export succeeds (validates page renders without runtime errors)
- `grep -r "getAllTips\|TipCard" src/app/page.tsx` returns empty (tips removed)
- `grep "Maker" src/app/page.tsx` returns metadata hit (identity updated)
- `grep "See Projects" src/components/home/Hero.tsx` returns CTA hit
</verification>

<success_criteria>
- Homepage hero communicates "maker & tinkerer" identity (not QA Engineer as lede)
- Homepage shows featured projects section with 2-3 project cards from Notion
- Homepage shows recent articles section with 2-3 article cards from Notion
- Homepage embeds AboutCard as inline short bio (HOME-04)
- Tips section fully removed from homepage
- Metadata title and description reflect maker identity
- All existing tests pass, build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/04-homepage-about/04-01-SUMMARY.md`
</output>
