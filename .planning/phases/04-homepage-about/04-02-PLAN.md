---
phase: 04-homepage-about
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/about/page.tsx
  - src/app/layout.tsx
autonomous: true
requirements:
  - ABOU-01

must_haves:
  truths:
    - "Visiting /about presents Tommy's full maker bio — reads as a human story, not a resume"
    - "The About page hero subtitle leads with maker identity, not QA Engineer"
    - "The About page metadata description leads with maker, not QA Engineer"
    - "The global Schema.org Person object includes maker/electronics/photography alongside QA"
    - "The global layout metadata title reflects maker identity"
  artifacts:
    - path: "src/app/about/page.tsx"
      provides: "About page with maker-first narrative copy and updated hero subtitle"
      contains: "Maker"
    - path: "src/app/layout.tsx"
      provides: "Updated global metadata and Schema.org Person with maker identity"
      contains: "Maker"
  key_links:
    - from: "src/app/layout.tsx"
      to: "Schema.org Person"
      via: "siteSchemas JSON-LD script"
      pattern: "knowsAbout.*Electronics"
    - from: "src/app/about/page.tsx"
      to: "src/components/home/AboutCard.tsx"
      via: "AboutCard import and render"
      pattern: "import AboutCard"
---

<objective>
Rewrite the About page copy and global site metadata to reflect Tommy's maker identity instead of the QA-first positioning.

Purpose: The About page and global metadata (layout.tsx) still describe Tommy as "QA Engineer & Unreal Debug Vault." ABOU-01 requires the About page to read as a human story about a maker, and the global metadata/Schema.org must match. The AboutCard component is already perfect — only the surrounding page copy, hero subtitle, and metadata need updating.

Output: Updated `src/app/about/page.tsx` (maker-first hero subtitle, updated metadata) and `src/app/layout.tsx` (global metadata title/description, Schema.org Person with expanded knowsAbout).
</objective>

<execution_context>
@/Users/tommy.lahitte/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tommy.lahitte/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-homepage-about/04-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. -->

From src/components/home/AboutCard.tsx:
```typescript
export default function AboutCard()
// 'use client' component — self-contained, no props. Contains full bio paragraphs + interactive skill badges.
// Already has maker-first narrative: "I'm Tommy, a maker and tinkerer who can't resist taking things apart..."
// Do NOT modify this component.
```

From src/app/layout.tsx — siteSchemas structure:
```typescript
const siteSchemas = [
  {
    '@context': 'https://schema.org',
    '@type': 'Person',
    name: 'Tommy Lahitte',
    jobTitle: 'Senior QA Engineer',
    worksFor: { '@type': 'Organization', name: 'Epic Games' },
    knowsAbout: ['Unreal Engine', 'Game QA', 'Debugging', 'Functional Testing', 'Automation'],
    url: 'https://tommylahitte.com',
    image: 'https://tommylahitte.com/avatar/tommy-lahitte-480.webp',
    sameAs: ['https://github.com/tlahitte'],
  },
  {
    '@context': 'https://schema.org',
    '@type': 'WebSite',
    name: 'Tommy Lahitte Debug Vault',
    url: 'https://tommylahitte.com',
    description: 'QA and debugging field notes for Unreal Engine...',
    author: { '@type': 'Person', name: 'Tommy Lahitte' },
  },
]
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite About page hero subtitle and metadata</name>
  <files>src/app/about/page.tsx</files>
  <read_first>
    - src/app/about/page.tsx
    - src/components/home/AboutCard.tsx
    - .planning/phases/04-homepage-about/04-RESEARCH.md
  </read_first>
  <action>
Update `src/app/about/page.tsx` — metadata and hero subtitle only. Do NOT change the page structure, avatar section, or AboutCard rendering.

**Metadata — replace the existing metadata export:**
```typescript
export const metadata: Metadata = {
  title: 'About | Tommy Lahitte',
  description:
    'Maker and tinkerer who bridges art and technology. A decade of live shows, media-server engineering at VYV, and now Virtual Production with Unreal Engine at Epic Games.',
  alternates: {
    canonical: 'https://tommylahitte.com/about',
  },
  openGraph: {
    title: 'About | Tommy Lahitte',
    description:
      'Maker and tinkerer who bridges art and technology. A decade of live shows, media-server engineering at VYV, and now Virtual Production with Unreal Engine at Epic Games.',
    url: 'https://tommylahitte.com/about',
    type: 'website',
  },
}
```

**Hero subtitle (the `<p>` with accent/muted spans) — replace:**
From:
```tsx
<p className="mt-1 text-lg font-medium">
  <span className="text-accent">Senior QA Engineer</span>
  <span className="text-text-muted mx-2">·</span>
  <span className="text-text-muted">Epic Games</span>
</p>
```
To:
```tsx
<p className="mt-1 text-lg font-medium">
  <span className="text-accent">Maker &amp; Tinkerer</span>
  <span className="text-text-muted mx-2">·</span>
  <span className="text-text-muted">Art meets Technology</span>
</p>
```

Everything else (avatar, rotating glow, AboutCard section, page structure) stays exactly as-is.
  </action>
  <verify>
    <automated>grep -n "Maker" src/app/about/page.tsx && grep -n "Senior QA Engineer" src/app/about/page.tsx; echo "exit: $?"</automated>
  </verify>
  <acceptance_criteria>
    - about/page.tsx metadata description starts with "Maker and tinkerer" (not "QA Engineer")
    - about/page.tsx hero subtitle contains "Maker" in the text-accent span (not "Senior QA Engineer")
    - about/page.tsx hero subtitle contains "Art meets Technology" in the text-text-muted span
    - about/page.tsx does NOT contain the string "Senior QA Engineer" anywhere
    - about/page.tsx still imports and renders AboutCard
    - about/page.tsx still has the avatar section with rotating glow unchanged
    - about/page.tsx openGraph description matches the main description
  </acceptance_criteria>
  <done>About page hero and metadata lead with maker identity. "Senior QA Engineer" no longer appears on the page — Epic Games is mentioned in the metadata as context, not as the lede.</done>
</task>

<task type="auto">
  <name>Task 2: Update global layout.tsx metadata and Schema.org Person</name>
  <files>src/app/layout.tsx</files>
  <read_first>
    - src/app/layout.tsx
    - .planning/phases/04-homepage-about/04-RESEARCH.md
  </read_first>
  <action>
Update `src/app/layout.tsx` — metadata and siteSchemas only. Do NOT change the layout structure, font config, Header/Footer imports, or body classes.

**Global metadata — replace the existing metadata export:**
```typescript
export const metadata: Metadata = {
  metadataBase: new URL('https://tommylahitte.com'),
  title: {
    default: 'Tommy Lahitte | Maker & Tinkerer',
    template: '%s | Tommy Lahitte',
  },
  description:
    'Tommy Lahitte — maker who bridges art and technology. Projects, field notes, and things worth building.',
  openGraph: {
    siteName: 'Tommy Lahitte',
    locale: 'en_US',
    type: 'website',
    images: [{ url: '/og-image.png', width: 1200, height: 630, alt: 'Tommy Lahitte — Maker & Tinkerer' }],
  },
  twitter: {
    card: 'summary_large_image',
    images: ['/og-image.png'],
  },
  robots: {
    index: true,
    follow: true,
    googleBot: { index: true, follow: true, 'max-image-preview': 'large' },
  },
}
```

Key changes from current:
- `title.default`: "Tommy Lahitte | Maker & Tinkerer" (was "QA Engineer & Unreal Debug Vault")
- `title.template`: "%s | Tommy Lahitte" (was "%s | Tommy Lahitte Debug Vault")
- `description`: maker-first (was QA-first)
- `openGraph.siteName`: "Tommy Lahitte" (was "Tommy Lahitte Debug Vault")
- `openGraph.images[0].alt`: "Tommy Lahitte — Maker & Tinkerer" (was QA branding)

**siteSchemas — replace the existing siteSchemas array:**
```typescript
const siteSchemas = [
  {
    '@context': 'https://schema.org',
    '@type': 'Person',
    name: 'Tommy Lahitte',
    jobTitle: 'Senior QA Engineer',
    worksFor: { '@type': 'Organization', name: 'Epic Games' },
    knowsAbout: [
      'Unreal Engine',
      'Game QA',
      'Debugging',
      'Functional Testing',
      'Automation',
      'Electronics',
      'Film Photography',
      'Media Servers',
      'Live Show Technology',
      'Virtual Production',
    ],
    url: 'https://tommylahitte.com',
    image: 'https://tommylahitte.com/avatar/tommy-lahitte-480.webp',
    sameAs: ['https://github.com/tlahitte'],
  },
  {
    '@context': 'https://schema.org',
    '@type': 'WebSite',
    name: 'Tommy Lahitte',
    url: 'https://tommylahitte.com',
    description:
      'Tommy Lahitte — maker who bridges art and technology. Projects, field notes, and things worth building.',
    author: { '@type': 'Person', name: 'Tommy Lahitte' },
  },
]
```

Key changes:
- Person `knowsAbout` expanded with: 'Electronics', 'Film Photography', 'Media Servers', 'Live Show Technology', 'Virtual Production'
- WebSite `name`: "Tommy Lahitte" (was "Tommy Lahitte Debug Vault")
- WebSite `description`: maker-first
- Person `jobTitle` stays "Senior QA Engineer" — Schema.org jobTitle is factual

Do NOT change: font config, body classes, Header/Footer imports, RootLayout function structure, html lang attribute.
  </action>
  <verify>
    <automated>grep -n "Maker" src/app/layout.tsx && grep -n "Electronics" src/app/layout.tsx && grep -n "Debug Vault" src/app/layout.tsx; echo "exit: $?"</automated>
  </verify>
  <acceptance_criteria>
    - layout.tsx metadata title.default contains "Maker & Tinkerer" (not "QA Engineer & Unreal Debug Vault")
    - layout.tsx metadata title.template is "%s | Tommy Lahitte" (not "%s | Tommy Lahitte Debug Vault")
    - layout.tsx metadata description contains "maker who bridges art and technology"
    - layout.tsx does NOT contain the string "Debug Vault" anywhere
    - layout.tsx siteSchemas Person knowsAbout array contains "Electronics" and "Film Photography" and "Virtual Production"
    - layout.tsx siteSchemas WebSite name is "Tommy Lahitte" (not "Tommy Lahitte Debug Vault")
    - layout.tsx still has metadataBase set to https://tommylahitte.com
    - layout.tsx still imports Header and Footer
    - layout.tsx still has the localFont denim config unchanged
    - pnpm vitest run exits 0
    - pnpm run build exits 0
  </acceptance_criteria>
  <done>Global metadata and Schema.org reflect maker identity. "Debug Vault" branding fully removed from layout. Schema.org Person knowsAbout expanded to cover maker facets (electronics, photography, media servers, live shows, virtual production).</done>
</task>

</tasks>

<verification>
- `pnpm vitest run` — all existing tests pass
- `pnpm run build` — static export succeeds (validates all pages render)
- `grep -r "Debug Vault" src/app/layout.tsx src/app/about/page.tsx` returns empty
- `grep "Maker" src/app/about/page.tsx src/app/layout.tsx` returns hits in both files
- `grep "Electronics" src/app/layout.tsx` confirms expanded knowsAbout
</verification>

<success_criteria>
- About page reads as a maker story (hero subtitle: "Maker & Tinkerer", not "Senior QA Engineer")
- About page metadata description leads with maker identity
- Global layout metadata title reflects "Maker & Tinkerer" branding
- "Debug Vault" branding fully removed from layout.tsx and about/page.tsx
- Schema.org Person includes electronics, photography, and live show technology
- All tests pass, build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/04-homepage-about/04-02-SUMMARY.md`
</output>
